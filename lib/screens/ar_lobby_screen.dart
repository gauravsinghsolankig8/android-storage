import 'package:flutter/material.dart';

class ARLobbyScreen extends StatelessWidget {
  const ARLobbyScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('AR Lobby')),
      body: const Center(
        child: Text('AR Lobby - Coming Soon!'),
      ),
    );
  }
}